{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the isolated desktop spike.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
